const PREFIX = 'ces/BrowseUploadsPage/';
//Or should it be it's own page? Makes sense as modal to me, 
//I mean either way it's making a new component so it's fine.
//can change it easily, I'll do stuff for sure on here first.
const FILTER_APPLIED = PREFIX + "FILTER_APPLIED";
const PAGE_TURNED = PREFIX + "PAGE_TURNED";
const UPLOADS_UPDATED = PREFIX + "UPLOADS_UPDATED";

//Will add more as needed.

export{

    FILTER_APPLIED,
    PAGE_TURNED,
    UPLOADS_UPDATED,
};