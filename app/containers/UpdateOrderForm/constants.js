//Honestly adding to OrderPrintPage prob not big deal, but clean separation is more organized;

const prefix = "cesprintingservice/UpdateOrderForm";

const UPDATE_ORDER = prefix+"UPDATE_ORDER";
const UPDATE_FAILED = prefix+"UPDATE_FAILED";
const UPDATE_SUCCESS = prefix+"UPDATE_SUCCESS";

export{

    UPDATE_ORDER,
    UPDATE_FAILED,
    UPDATE_SUCCESS,
};
